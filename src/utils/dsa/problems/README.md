# 编程题正文（本机保留，不进公开仓库）

`iteration.ts` / `recursion.ts` 由 `dsaCatalog.ts` 用 glob 加载，供本机开发。克隆仓库后若没有这两份文件，目录仍在，题列表先空着。

云端：本机执行 `npm run sync:cf-dsa`，题面进 `WENGU_KV`。Pages 打开 `/dsa` 会拉 `/api/dsa/bank`，面试或其他设备无需再带这两份文件。
