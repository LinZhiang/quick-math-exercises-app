# 本地：npm run setup 会从 server/.env 同步到 .env.local
#
# Cloudflare Pages 静态前端 + 家庭 Node API（推荐）：
#   1. 电脑运行 npm run serve:install
#   2. cloudflared tunnel --url https://localhost:8790  得到公网 HTTPS 地址
#   3. server/.env 填写：
#        WENGU_PUBLIC_API_URL=https://xxx.trycloudflare.com
#        CORS_ORIGIN=https://你的项目.pages.dev
#   4. Cloudflare Pages 环境变量：
#        VITE_WENGU_API_ORIGIN=https://xxx.trycloudflare.com
#   5. 重新部署 Pages
#
# VITE_WENGU_API_ORIGIN=https://xxx.trycloudflare.com
# VITE_DEEPSEEK_MODEL=deepseek-v4-flash
