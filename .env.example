# 本地：npm run setup / serve:install 读取 server/.env
#
# ========== 公网一步到位（推荐，出门不用开电脑）==========
# Cloudflare Pages → Settings → Environment variables（Production Secrets）：
#   DEEPSEEK_API_KEY=sk-...
#   WENGU_ADMIN_USERNAME=admin
#   WENGU_ADMIN_PASSWORD=你的强密码
#   WENGU_SESSION_SECRET=随机长字符串（可选但推荐）
# 可选：Pages → Settings → Functions → KV bindings
#   Variable name: WENGU_KV （用于成员账号管理）
# 然后重新部署。家人只打开同一个 pages.dev 地址即可登录。
#
# VITE_DEEPSEEK_MODEL=deepseek-v4-flash
