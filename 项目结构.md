# 项目结构说明（接手指南）

本文说明「学习 App」代码怎么分层、改完这次结构优化之后该从哪读起。更短的命令与模块介绍见根目录 `README.md`。

## 这是什么

Vue 3 + Vite + Element Plus 的练习站。首页四个入口：

1. **知识训练** `/train/:section` — 口算、快判、数量关系、资料分析、逻辑判断、语文
2. **题库整理** `/bank` — 个人题库
3. **计算机基础** `/computer` — 讲义树、问 AI、AI 测验
4. **前端学习** — 尚未开放

手机端部署在 Cloudflare Pages（`pages.dev`）。安装 / 设置按钮**只在首页**顶栏出现。

## 先看这几处

| 想了解 | 打开 |
| --- | --- |
| 路由与页面入口 | `src/router/index.ts` |
| 首页卡片 | `src/views/home/HomeHub.vue` |
| 知识训练总壳 | `src/views/tools/mental-math/index.vue`（样式在同目录 `mental-math-page.css`） |
| 语文 tab | `src/views/tools/chinese-practice/` |
| 计算机基础树 | `src/views/computer-basics/` |
| AI 出题（对外只这一处） | `src/services/deepseek.ts` |
| 练习高亮规则 | `.cursor/rules/practice-highlight-flow.mdc` |

页面不要直接 `import` `src/services/quiz/*`。一律：

```ts
import { isAiChatConfigured, requestIdiomRecognitionMcqs } from '@/services/deepseek'
```

## 目录地图

```
src/
  main.ts                 应用入口（SW、主题）
  App.vue                 顶栏壳
  router/                 路由
  views/                  页面
  components/             跨模块小组件（富文本、PWA 安装等）
  composables/            各题型「出题/提交/错题」逻辑（已按领域分子目录）
  utils/                  纯函数、题库、存储（已按领域分子目录）
  services/               AI 与 HTTP
  constants/              tab / 攻略文案
  data/                   诗人速览、时政材料等静态资料
functions/                Cloudflare Pages 服务端
server/                   本地 AI / 登录代理（dev:full）
public/                   静态资源、PWA、计算机基础快照 cb-data/
```

### `src/utils/`（原近 200 个文件平铺）

| 子目录 | 放什么 |
| --- | --- |
| `app/` | 导航、设置、备份、练习统计、AI Key、JSON 解析 |
| `chinese/` | 语文出题、本地大题库、错题存储 |
| `computer/` | 讲义树、测验、温故登录 API |
| `data-analysis/` | 资料分析各考点；公式展示 `dataAnalysisMathDisplay.ts` |
| `logic/` | 翻译、加强削弱、真假、图形推理等 |
| `markdown/` | Markdown / 富文本转 HTML |
| `math/` | 数量关系、口算、舒尔特 |
| `personal-bank/` | 个人题库导入导出 |

引用写成 `@/utils/math/mentalMathPractice`，不要再写 `@/utils/mentalMathPractice`。

### `src/composables/`

与 utils 对齐：`app/` `chinese/` `data-analysis/` `logic/` `math/` `computer/`。

一个 `useXxxTest.ts` 对应一块练习面板。改出题流程优先改 composable，而不是把逻辑堆进 `index.vue`。

### `src/views/tools/mental-math/components/`

| 子目录 | 面板 |
| --- | --- |
| `shared/` | 错题本、收藏、完成统计、攻略 |
| `math/` | 数量 / 口算 / 舒尔特等 |
| `data-analysis/` | 资料分析 |
| `logic/` | 逻辑判断 |
| `chinese/` | 圈病句、压缩语段、系统管理导图 |

### `src/services/`

- `ai.ts` — DeepSeek / 豆包 Chat Completions
- `aiQuizCore.ts` — 是否已登录可调 AI、对话、计算机讲义测验
- `quiz/chineseMcq.ts` `dataAnalysisMcq.ts` `mathLogicMcq.ts` — 各科出题 prompt
- `deepseek.ts` — 上面这些的 **re-export 入口**

## 超长文件怎么理解

没有把「一道题的出题器」拆成碎片，避免改一处考点要跳十个文件。仍然偏长的，多半是 **prompt 或题库数据**：

| 文件 | 原因 |
| --- | --- |
| `services/quiz/dataAnalysisMcq.ts` | 资料分析各考点的 system prompt + 校验 |
| `services/quiz/chineseMcq.ts` | 语文各题型 prompt |
| `utils/chinese/hanziPatternBank.ts` 等 `*Bank.ts` | 生成器产出的题库，不要手改整表 |
| `constants/mathOpStrategyGuides.ts` | 运算技巧攻略文案 |
| `mental-math/index.vue` | 总壳仍负责切换题型；样式已抽到 css |

本地大题库由 `scripts/generate-*.mjs` 生成。改种子后重跑对应脚本，再提交生成物。

## 资料分析高亮

提交后圈材料/题干/解析，以解析真正用到的专名和数据为准。实现参考 `src/utils/data-analysis/dataAnalysisPractice.ts`。增长类与「百分数与百分点」错题分区隔离；增长出题强制豆包。细则见 `.cursor/rules/practice-highlight-flow.mdc`。

## 计算机基础与云端

- 前端树：`src/utils/computer/computerBasics.ts`
- Pages Functions：`functions/api/computer-basics/`
- 鉴权：`functions/_lib/wenguCloudAuth.js`（不要重复 `export function json`）
- 本地联调：`npm run dev:full`（Vite + `server/`）

## 新增一种练习（建议顺序）

1. `src/utils/<领域>/xxxPractice.ts` — 出题、判分、类型
2. `src/composables/<领域>/useXxxTest.ts` — 加载、提交、错题
3. `src/views/tools/mental-math/components/<领域>/XxxPanel.vue` — UI
4. 在 `src/constants/practice-hub-sections.ts` 和 `index.vue` 登记
5. 若走 AI：在 `src/services/quiz/` 对应文件加 `requestXxxMcqs`，从 `deepseek.ts` 自动导出

语文练习走 `chinese-practice-tabs.ts` + `views/tools/chinese-practice/`，模式相同。
