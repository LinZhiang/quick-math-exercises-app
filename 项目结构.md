# 项目结构说明

Vue 3 + TypeScript + Vite + Element Plus 的学习 App，网页和 PWA 共用同一套页面。本文按**现在首页上实际有的模块**来说；知识训练只点一下，后面几块才是重点。更短的命令见根目录 `README.md`。

登录、安装、设置在首页右上角。卡片显示哪些，在设置「菜单管理」里开关。题库整理始终在；知识训练、计算机单词和语法默认关掉。

## 首页现在有什么

固定两张（不进菜单开关）：

1. **项目介绍** `/about` — 只介绍当前开着的模块，底部有技术栈
2. **项目管理** `/project-code` — 仅管理员，源码目录查阅 / 下 Word

菜单里的模块（顺序与 `src/constants/home-hub-modules.ts` 一致）：

3. **知识训练** `/train/:section` — 默认隐藏
4. **题库整理** `/bank` — 始终显示
5. **计算机基础** `/computer`
6. **前端学习** `/frontend`
7. **计算机单词和语法** `/cs-vocab` — 默认隐藏
8. **数据结构与算法** `/dsa`

游客能看已公开讲义、本机整理题目；成员登录后可用测验 / 提问（须自备接口）；管理员管目录、公开范围、成员和项目管理。

---

## 知识训练（略）

首页默认关闭。打开后是限时练习壳：口算、快判、数量关系、资料分析、逻辑判断、语文，带日志和错题。

入口：`src/views/tools/mental-math/`。AI 出题对外只走 `src/services/deepseek.ts`（内部再分到 `services/quiz/`）。提交后高亮规则见 `.cursor/rules/practice-highlight-flow.mdc`。本文不再展开。

---

## 项目介绍

**有什么：** 手风琴说明。先讲项目本身和游客 / 成员 / 管理员，再按首页当前可见卡片逐个介绍，最后一节「用到的技术」始终在。设置里关掉的模块不会出现，避免介绍和菜单对不上。

**用了哪些：**

- 文案拆在 `src/constants/projectIntro/`（`overview` / `modules` / `stack`）
- 可见性跟首页同一套 `src/utils/app/homeHubMenu.ts`
- 页面 `src/views/home/ProjectIntroPage.vue`

---

## 题库整理

**有什么：** 自己建大类 / 小类，收简答题和选择题。可手打，也可拍照识别后核对再存。小类里开测验（计时、打乱、简答自评）。单题或分类可导出 Word，整库可在设置里备份 JSON。

**用了哪些：**

- 树和题目存在本机（`personal-question-bank-v1`），登录后可随 user-kv 同步：`src/utils/personal-bank/personalQuestionBank.ts`
- 题干 / 答案 / 解析是富文本，写入前 `DOMPurify` 过滤：`src/utils/markdown/richTextHtml.ts`
- 拍照不是本地 OCR 库，而是把裁切后的图交给服务端 AI 抽题：`personalBankPhotoExtract.ts`
- 导出 Word：`docx` + `personalBankDocxHtml.ts` / `personalBankWordExport.ts`（代码块、公式、分页和讲义导出共用这套 HTML→Word）
- 页面：`src/views/personal-bank/`

---

## 计算机基础

**有什么：** 树形讲义。可折叠目录；未开放显示「即将开放」；私密条目只有管理员看得到。点进正文阅读（代码高亮、插图、备注），可全屏、上下翻篇。讲义内可测验、按当前篇提问。目录上有题目整理和学习日志。单篇或勾选批量下 Word：多篇合成一份，上一篇结束后另起一页，不另加封面标题。管理员可增删改分类和正文、移动位置；新建默认私密，讲义页「设为公开」后别人才看得到。

**用了哪些：**

- 前端目录 / 正文 API：`src/utils/computer/computerBasics.ts`（不把讲义写死在前端）
- 本机 Node：`server/computer-basics-store.mjs`；云端 Pages Functions + `WENGU_KV`：`functions/_lib/computerBasicsCloud.js`
- 登录鉴权共用 `wenguAuthStore` + `functions/_lib/wenguCloudAuth.js`；请求走 `wenguApiFetch`（默认同源，密钥只在服务端）
- 正文 Markdown → HTML：`marked`，展示前 `DOMPurify`；代码高亮复用 `src/utils/dsa/highlightTs.ts`
- 导出：`src/utils/markdown/handoutExport.ts`（`docx`，`pageBreakFirst`）；批量勾选 `HandoutBatchDownloadDialog.vue`
- 灌云：`npm run sync:cf-computer`
- 页面：`src/views/computer-basics/`

讲义正文、题库、`server/data` **不进 Git**。

---

## 前端学习

**有什么：** 和计算机基础同一套产品（目录树、阅读、测验、问答、批量 Word、公开/私密），内容换成前端专题。操作对齐，避免两套习惯。

**用了哪些：**

- 前端镜像：`src/utils/frontend/frontendLearning.ts`，页面 `src/views/frontend-learning/`（组件命名与计算机基础对应）
- 云端另一套 KV 前缀：`functions/_lib/frontendLearningCloud.js`
- 导出、鉴权、高亮、过滤与计算机基础共用，不另起一套 Word / 登录
- 灌云：`npm run sync:cf-frontend`

改交互时两棵树一起改，只换科目内容和接口路径。

---

## 计算机单词和语法

**有什么：** 首页默认关掉。不是另编单词书，而是从计算机基础、前端学习讲义里抽核心词、语法和必记说法，一词一点。点目录先识记，再限时测；错了进错题。

**用了哪些：**

- 题型目录：`src/utils/cs-vocab/csVocabTopics.ts`
- 题库加载：`csVocabBank.ts`（优先接口，其次本机 `bank.generated.json`；生成物不进公开仓库）
- 云端 KV：`functions/_lib/csVocabCloud.js`；灌云 `npm run sync:cf-cs-vocab`
- 页面：`src/views/cs-vocab/CsVocabView.vue`

---

## 数据结构与算法

**有什么：** 大类 / 小类选题。目前是「复杂度分析」下的迭代、递归。题目只给 TypeScript 函数签名和目的，方法体自己补；提交后在本机跑测试看对错，可反复测，日志记通过情况。编辑区高亮接近编辑器观感，没有上 Monaco / CodeMirror。

**用了哪些：**

- 目录与题目：`src/utils/dsa/dsaCatalog.ts`；题目文件用 `import.meta.glob('./problems/*.ts')` 加载（题面不进 Git）
- 起始代码：`buildDsaProblem.ts`（签名 +「目的」注释，光标落在函数体）
- 高亮：自写分词 `highlightTs.ts`（`tok-*` 色，讲义代码块和项目管理查看也用它）
- 执行：先 `stripTypeScript.ts` 剥类型再跑；测试在 Web Worker 里 `new Function`，避免卡住页面：`runJsTests.ts`
- 页面：`src/views/dsa/`，编辑区 `DsaHighlightedCode.vue`

没有引入完整 TS 编译器，只覆盖练习会写到的标注。

---

## 项目管理（管理员）

**有什么：** 首页仅管理员多一张卡。把指定项目里的 vue / js / ts 等收成目录树，点文件像编辑器那样看，可单文件或批量下 Word。批量是一份 docx，每个源文件另起一页，页首写文件名和文件地址。源码不进 Git。

**用了哪些：**

- 本机拷贝：`npm run sync:project-code` → gitignored 的 `server/data/project-code/`
- 本机接口：`server/project-code-store.mjs`（管理员 tree / file / files）
- 云端查阅：`functions/_lib/projectCodeCloud.js`，正文进 `WENGU_KV`；灌云 `npm run sync:cf-project-code`
- 查看高亮：`projectCodeHighlight.ts` → `highlightTs`
- Word：`projectCodeDocx.ts`（标题 + 路径 + 高亮 `<pre>`，分页同讲义批量导出）
- 页面：`src/views/project-code/`，批量勾选 `ProjectCodeBatchDialog.vue`

---

## 这几块共用的技术

| 层 | 用什么 |
| --- | --- |
| 界面 | Vue 3、TypeScript、Vite、Vue Router、Element Plus |
| 安装 | PWA（manifest + Service Worker），网页和主屏同一套路由 |
| 正文 | `marked` 渲染，`DOMPurify` 过滤 |
| Word | `docx`，HTML 块在 `personalBankDocxHtml.ts` 里转段落 / 代码表，讲义、题库、源码导出共用 |
| 登录 / AI | 本机 `npm run dev:full` 起 Node（`server/`）；线上 Cloudflare Pages Functions。密钥只放服务端环境变量 |
| 讲义持久化 | 本机 `server/data`；云端 `WENGU_KV`。私密内容不进 Git |
| 账号数据 | 练习记录、错题、题库、菜单习惯：未登录本机；登录后 user-kv |

AI 对话统一 `src/services/ai.ts`，页面不要直接打模型厂商。

---

## 先看这几处

| 想了解 | 打开 |
| --- | --- |
| 路由 | `src/router/index.ts` |
| 首页卡片 | `src/views/home/HomeHub.vue`、`src/constants/home-hub-modules.ts` |
| 项目介绍 | `src/constants/projectIntro/` |
| 题库整理 | `src/views/personal-bank/`、`src/utils/personal-bank/` |
| 计算机基础 | `src/views/computer-basics/`、`src/utils/computer/computerBasics.ts` |
| 前端学习 | `src/views/frontend-learning/`、`src/utils/frontend/frontendLearning.ts` |
| 单词语法 | `src/views/cs-vocab/`、`src/utils/cs-vocab/` |
| 数据结构 | `src/views/dsa/`、`src/utils/dsa/` |
| 项目管理 | `src/views/project-code/`、`functions/_lib/projectCodeCloud.js` |
| 鉴权 / 请求 | `src/utils/computer/wenguAuthStore.ts`、`wenguApiFetch.ts` |
| Word 导出 | `src/utils/markdown/handoutExport.ts` |

## 目录地图

```
src/
  views/home/             首页、项目介绍
  views/personal-bank/    题库整理
  views/computer-basics/  计算机基础
  views/frontend-learning/ 前端学习（与上一套对齐）
  views/cs-vocab/         计算机单词和语法
  views/dsa/              数据结构与算法
  views/project-code/     项目管理（管理员）
  views/tools/mental-math/ 知识训练总壳
  views/common/           登录、安装设置
  utils/personal-bank/    题库存储、拍照抽题、Word
  utils/computer/         讲义 API、登录、学习日志
  utils/frontend/         前端学习 API（镜像计算机基础）
  utils/cs-vocab/         单词题库
  utils/dsa/              题目目录、高亮、剥 TS、Worker 测题
  utils/project-code/     源码 API、高亮、Word
  utils/markdown/         Markdown / 过滤 / 讲义导出
  constants/projectIntro/ 项目介绍文案
  services/ai.ts          登录后的 Chat Completions
functions/                Cloudflare Pages
server/                   本地登录 / 讲义 / AI 代理（dev:full）
```

改讲义交互：计算机基础和前端学习成对改。改代码高亮：优先 `highlightTs.ts`。改 Word 分页或代码块样式：优先 `personalBankDocxHtml.ts` 和 `handoutExport.ts`。
